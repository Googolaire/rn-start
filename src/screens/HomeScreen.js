import React from 'react';
import { Text, StyleSheet, TextInput } from 'react-native';

const HomeScreen = () => {
  return (
    <>
  <Text style={styles.text}>instaEStore</Text>
    </>
    );
};

const styles = StyleSheet.create({
  text: {
    fontSize: 30
  }
});

export default HomeScreen;
